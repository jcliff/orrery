{
  "name": "orrery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter hurricanes dev",
    "pipeline": "pnpm --filter fieldline pipeline"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
